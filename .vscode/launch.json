{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}/server.js",  // Adjust the filename accordingly
            "skipFiles": [
                "<node_internals>/**"
            ]
        }
    ]
}
